<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TODo"
        tools:targetApi="31">
        <activity
            android:name=".Splash_Screen"
            android:exported="true"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/Theme.TODo" >
        </activity>
        <activity
            android:name=".TaskProperty"
            android:exported="false"
            android:label="@string/title_activity_task_property"
            android:theme="@style/Theme.TODo" />
        <activity
            android:name=".BottomNavActivities.MineActivity"
            android:exported="false"
            android:label="@string/title_activity_mine"
            android:theme="@style/Theme.TODo" />
        <activity
            android:name=".BottomNavActivities.CalenderActivity"
            android:exported="false"
            android:label="@string/title_activity_calender"
            android:theme="@style/Theme.TODo" />
        <activity
            android:name=".BaseActivity"
            android:exported="false"
            android:label="@string/title_activity_base"
            android:theme="@style/Theme.TODo" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TODo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>